# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
*.local

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?

# Raw NetCDF source files (heavy, not needed in repo)
data/raw/

# Data files — served from Cloudflare R2 in prod, local dev reads from disk
public/data/**/*.bin
public/data/**/*.json
public/data/ne_10m_land.geojson
!public/data/backup/*.json
!public/data/index.json

# Local env (secrets, deploy overrides)
.env
.env.local
.env.*.local

# Local Claude Code permissions (per-machine)
.claude/settings.local.json

# Misc local junk
Test chrome JSON/
IMG-*.jpg
scripts/*.log
scripts/convert_log.txt

